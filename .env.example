# Local environment variables for OpenClaw gateway (Docker Compose).
# Copy this file to .env.local and set real values. .env.local is git-ignored
# and excluded from Docker build context.
#
#   cp .env.example .env
#
# Do not commit .env or put secrets in this example file.

# Amazee.ai LLM API
AMAZEEAI_BASE_URL=https://llm.us104.amazee.ai
AMAZEEAI_API_KEY=
AMAZEEAI_DEFAULT_MODEL=claude-4-5-sonnet
